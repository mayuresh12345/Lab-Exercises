#include "mpi.h"
#include <stdio.h>
#include <stdlib.h>

int main()
{
	int rank, size, x;
	MPI_Status status;
		MPI_Init(&argc, &argv);
		MPI_Comm_rank(MPI_COMM_WORLD, &rank);
		MPI_Comm_size(MPI_COMM_WORLD, &size);
		if(rank == 0)
		{
			scanf("%d", &x);
}
